<!-- app/views/flats/index.html.erb -->
<h1>Flats</h1>
<ul>
  <% @flats.each do |flat| %>
    <li>
      <img src="<%= flat["imageUrl"] %>" alt="">
      <h2><%= flat["name"] %></h2>
      <p>💰 <%= flat["price"] %> <%= flat["priceCurrency"] %></p>
    </li>
  <% end %>
</ul>
